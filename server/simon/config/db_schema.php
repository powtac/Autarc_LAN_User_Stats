<?php

// For setup only

//$schema[] = 'CREATE  TABLE  IF NOT EXISTS "devices"   ("id" INTEGER PRIMARY KEY  AUTOINCREMENT  NOT NULL , "mac_vendor" CHAR NOT NULL , "mac_md5" CHAR)';
//$schema[] = 'CREATE  TABLE  IF NOT EXISTS "usage"     ("id" INTEGER PRIMARY KEY  AUTOINCREMENT  NOT NULL , "hit" DATETIME NOT NULL  DEFAULT CURRENT_TIMESTAMP)';
//$schema[] = 'CREATE  TABLE  IF NOT EXISTS "changelog" ("id" INTEGER PRIMARY KEY  AUTOINCREMENT  NOT NULL , "devices_id" INTEGER NOT NULL , "type" CHAR, "change" CHAR)';
